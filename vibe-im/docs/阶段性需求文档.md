**完善计划**

1. **重构左侧信息架构**
   - 将现有单侧栏改为：`一级图标导航栏 + 当前模块列表栏`。
   - 一级导航包含：消息、联系人、智能体、群组、管理后台/设置。
   - 删除顶部拥挤的 `聊天 / 好友 / 智能体 / 群聊` Tab。

2. **消息模块**
   - 默认进入 `消息`。
   - 列表只展示最近会话。
   - 顶部保留全局搜索和新建入口。
   - Agent 会话仍可出现在最近会话里，但不作为顶部 Tab。

3. **联系人模块**
   - 独立展示好友列表。
   - 顶部支持搜索用户。
   - 搜索结果支持添加好友、发起单聊。
   - 后续可扩展新朋友、分组、在线状态。

4. **智能体模块**
   - 作为独立一级入口。
   - 展示 Vibe Claw 连接状态。
   - 支持 Agent 搜索、分类筛选、同步智能体。
   - 已对话过的 Agent 显示“继续对话”，未对话显示“开始对话”。

5. **群组模块**
   - 独立展示群聊列表。
   - 顶部提供创建群聊入口。
   - 群组列表不再混在最近聊天 Tab 中。

6. **账号与管理入口**
   - 用户信息保留在侧栏顶部或底部，但减少占用。
   - 管理后台入口移到底部设置/管理区域。
   - 退出按钮放入用户操作区，避免和主导航混在一起。

7. **响应式适配**
   - 桌面端：左侧双栏。
   - 移动端：一级导航改为底部导航，列表区和聊天区上下切换。
   - 保持聊天窗口不跳动、不挤压。

8. **回归验证**
   - 验证最近会话点击进入聊天。
   - 验证好友搜索、添加、发起单聊。
   - 验证 Agent 搜索、筛选、同步、进入对话。
   - 验证群聊列表和创建群聊。
   - 验证管理后台入口和退出逻辑。
   - 浏览器完整回归一次。