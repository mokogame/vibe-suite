export const adminHtml = `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Vibe Claw Admin</title>
  <style>
    :root{--ink:#0f172a;--muted:#64748b;--line:#dbe4f0;--paper:#fff;--bg:#f4f7fb;--blue:#2563eb;--blue-dark:#1e3a8a;--blue-soft:#eff6ff;--side:#0f2747;--bad:#dc2626;--ok:#059669;--mono:"SFMono-Regular","Cascadia Code","Consolas",monospace;--body:"Aptos","Segoe UI","PingFang SC","Microsoft YaHei",sans-serif}
    *{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:var(--body);min-height:100vh;font-size:14px}.shell{display:grid;grid-template-columns:260px 1fr;min-height:100vh}.side{position:sticky;top:0;height:100vh;padding:24px 18px;background:var(--side);color:#eaf2ff;border-right:1px solid rgba(255,255,255,.08);overflow:auto}.brand{font-size:22px;line-height:1.15;margin:0;font-weight:800;letter-spacing:.2px}.brand-sub{margin:6px 0 22px;color:#9fb5d1;font-size:12px}.token-card{padding:14px;border:1px solid rgba(255,255,255,.12);border-radius:14px;background:rgba(255,255,255,.06)}.token-help{margin:8px 0 0;color:#9fb5d1;font-size:12px;line-height:1.45}label{display:block;margin:10px 0 6px;font-size:12px;color:var(--muted);font-weight:700}.side label{color:#b8c9e2}.nav{display:grid;gap:6px;margin-top:18px}.nav button{justify-content:flex-start;width:100%;background:transparent;color:#dbeafe;border:1px solid transparent;border-radius:10px;padding:10px 12px;font-weight:700}.nav button:hover{background:rgba(255,255,255,.08);transform:none}.nav button.active{background:#fff;color:var(--blue-dark);box-shadow:0 1px 2px rgba(15,23,42,.08)}.main{height:100vh;padding:24px;overflow:hidden;display:flex;flex-direction:column}.hero{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(280px,.75fr);gap:16px;margin-bottom:18px}#sections{flex:1;min-height:0;overflow:auto}.section.active{display:block}.section.active.chat-page{height:100%;display:flex;flex-direction:column;min-height:0;overflow:hidden}.hero-card,.panel,.result,.modal-card{background:var(--paper);border:1px solid var(--line);border-radius:16px;box-shadow:0 1px 3px rgba(15,23,42,.06)}.hero-card{padding:22px;overflow:hidden}.eyebrow{display:inline-flex;gap:8px;align-items:center;padding:5px 9px;border-radius:999px;background:var(--blue-soft);color:var(--blue-dark);font-size:12px;font-weight:800}.hero h1{font-size:28px;line-height:1.18;margin:14px 0 8px;letter-spacing:-.4px;font-weight:800}.muted{color:var(--muted);line-height:1.65}.status-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;padding:16px}.metric{padding:14px;border-radius:12px;background:#f8fbff;border:1px solid var(--line)}.metric b{display:block;font-size:26px;line-height:1.1;font-weight:800;color:var(--blue-dark)}.metric span{font-size:12px;color:var(--muted);font-weight:700}.toolbar,.section-head{display:flex;gap:10px;align-items:center;justify-content:space-between;margin:18px 0 12px}.tabs-title{font-size:22px;margin:0;font-weight:800}.grid{display:grid;grid-template-columns:1.05fr .95fr;gap:16px}.panel{padding:18px;min-width:0}.panel h2{font-size:18px;margin:0 0 6px;font-weight:800}.panel p{margin:0 0 14px}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}input,textarea,select{width:100%;border:1px solid #cbd5e1;border-radius:10px;padding:10px 11px;background:#fff;color:var(--ink);font:inherit;outline:none}input:focus,textarea:focus,select:focus{border-color:var(--blue);box-shadow:0 0 0 3px rgba(37,99,235,.12)}textarea{min-height:92px;resize:vertical}.wide{grid-column:1/-1}button{border:0;border-radius:10px;background:var(--blue);color:#fff;padding:10px 14px;font-weight:800;cursor:pointer;display:inline-flex;align-items:center;gap:8px;transition:.14s background,.14s box-shadow}button:hover{background:#1d4ed8;transform:none}button:disabled{cursor:not-allowed;opacity:.45;background:#94a3b8}button:disabled:hover{background:#94a3b8}button.secondary{background:#e8eef7;color:#1e3a8a}button.secondary:hover{background:#dbe8ff}button.warn{background:var(--bad)}button.ghost{background:#fff;color:var(--blue-dark);border:1px solid var(--line)}button.ghost:hover{background:var(--blue-soft)}button.small{padding:7px 10px;font-size:12px}.list{display:grid;gap:10px;max-height:520px;overflow:auto;padding-right:4px}.item{border:1px solid var(--line);background:#fff;border-radius:12px;padding:12px}.item-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.item h3{margin:0;font-size:14px;font-weight:800}.badge{display:inline-flex;align-items:center;border-radius:999px;padding:4px 8px;background:#dcfce7;color:#166534;font-size:11px;font-weight:800}.badge.bad{background:#fee2e2;color:#991b1b}.code{font-family:var(--mono);font-size:12px;color:#475569;word-break:break-all;line-height:1.45}.section{display:none}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:14px;background:#fff}.data-table{width:100%;border-collapse:collapse;table-layout:fixed;min-width:860px}.data-table th,.data-table td{padding:12px;border-bottom:1px solid var(--line);text-align:left;vertical-align:middle;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.data-table th{font-size:12px;color:#475569;background:#f8fbff;text-transform:uppercase;letter-spacing:.03em}.data-table tr:last-child td{border-bottom:0}.data-table tr.clickable{cursor:pointer}.data-table tr.clickable:hover td{background:#f8fbff}.data-table .code{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;word-break:normal}.data-table .row-actions{flex-wrap:nowrap}.empty{padding:28px;color:var(--muted);text-align:center}.row-actions{display:flex;gap:8px;flex-wrap:wrap}.conversation-view{height:100%;display:flex;flex-direction:column;min-height:0}.conversation-query{margin-top:12px;flex:0 0 auto}.chat-shell{flex:1;min-height:0;display:grid;grid-template-rows:minmax(0,1fr) auto;background:#eef5ff;border:1px solid var(--line);border-radius:18px;overflow:hidden}.chat-history{min-height:0;padding:18px;overflow:auto;display:flex;flex-direction:column;gap:12px;background:linear-gradient(180deg,#f8fbff,#edf5ff)}.bubble{max-width:78%;padding:11px 13px;border-radius:16px;border:1px solid var(--line);background:#fff;box-shadow:0 1px 2px rgba(15,23,42,.04)}.bubble.user{align-self:flex-end;background:#2563eb;color:#fff;border-color:#2563eb}.bubble.agent{align-self:flex-start}.bubble.system{align-self:center;max-width:92%;background:#fff7ed;color:#9a3412}.bubble-meta{font-size:11px;opacity:.76;margin-bottom:4px;font-weight:800}.bubble-text{white-space:pre-wrap;line-height:1.55}.chat-composer{padding:12px;background:#fff;border-top:1px solid var(--line);display:grid;gap:10px}.composer-mini{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:10px;align-items:end}.composer-attach input{width:220px}.composer-message textarea{min-height:42px;height:42px;max-height:160px;overflow:hidden}.composer-row{display:grid;grid-template-columns:1fr 180px;gap:10px}.composer-send{display:grid;grid-template-columns:1fr auto;gap:10px;align-items:end}.attachments{font-size:12px;color:var(--muted)}.agent-dialog{display:grid;gap:14px}.recent-list{display:grid;gap:8px}.recent-card{width:100%;display:grid;grid-template-columns:1fr auto;gap:12px;align-items:center;text-align:left;border:1px solid var(--line);background:#fff;color:var(--ink);border-radius:12px;padding:12px}.recent-card:hover{background:#f8fbff}.recent-preview{font-size:13px;line-height:1.45;color:var(--ink);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.recent-meta{font-size:11px;color:var(--muted);margin-top:4px}.agent-chat-box{height:min(62vh,720px);display:grid;grid-template-rows:minmax(0,1fr) auto;border:1px solid var(--line);border-radius:14px;overflow:hidden;background:#eef5ff}.agent-chat-box .chat-history{min-height:0}.modal-chat-composer{padding:10px;background:#fff;border-top:1px solid var(--line);display:grid;grid-template-columns:1fr auto;gap:10px;align-items:end}.modal-chat-composer textarea{min-height:42px;height:42px;max-height:140px;overflow:hidden}.modal{position:fixed;inset:0;background:rgba(15,23,42,.42);display:none;align-items:center;justify-content:center;padding:24px;z-index:40}.modal.open{display:flex}.modal-card{width:min(760px,100%);max-height:90vh;overflow:auto}.modal header{display:flex;justify-content:space-between;align-items:center;padding:16px 18px;border-bottom:1px solid var(--line)}.modal h2{margin:0;font-size:18px}.modal-body{padding:18px}.modal-actions{display:flex;gap:10px;justify-content:flex-end;margin-top:16px}.result{position:fixed;right:22px;bottom:22px;width:min(560px,calc(100vw - 44px));max-height:48vh;display:grid;grid-template-rows:auto 1fr;overflow:hidden;z-index:20}.result header{display:flex;align-items:center;justify-content:space-between;padding:11px 13px;background:#0f2747;color:#fff}.result header .split-actions{margin-top:0}.result pre{margin:0;padding:14px;overflow:auto;background:#0b1220;color:#dbeafe;font-family:var(--mono);font-size:12px}.result-toggle{position:fixed;right:22px;bottom:22px;z-index:19}.hidden{display:none}.split-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}.danger-text{color:var(--bad)}.ok-text{color:var(--ok)}@media(max-width:920px){.shell{grid-template-columns:1fr}.side{position:relative;height:auto}.hero,.grid{grid-template-columns:1fr}.form-grid,.composer-row,.composer-send,.composer-mini{grid-template-columns:1fr}.composer-attach input{width:100%}.main{height:auto;min-height:100vh;padding:16px}.hero h1{font-size:24px}.status-grid{grid-template-columns:1fr}.nav{grid-template-columns:repeat(2,minmax(0,1fr))}.chat-shell{min-height:70vh}.bubble{max-width:92%}}
  </style>
</head>
<body>
  <div class="shell">
    <aside class="side">
      <h1 class="brand">Vibe Claw</h1>
      <p class="brand-sub">Agent engineering cockpit</p>
      <nav class="nav" id="nav"></nav>
    </aside>
    <main class="main">
      <div class="toolbar"><h2 class="tabs-title" id="sectionTitle">概览</h2><div class="split-actions"><button class="secondary hidden" id="chatSettingsTop" type="button">会话设置</button></div></div>
      <div id="sections"></div>
    </main>
  </div>
  <aside class="result" id="result"><header><strong>API 响应</strong><div class="split-actions"><button class="secondary" id="copyLast" type="button">复制 API 响应</button><button class="secondary" id="hideResult" type="button">隐藏</button></div></header><pre id="out">等待操作...</pre></aside>
  <button class="result-toggle secondary hidden" id="showResult" type="button">API 响应</button>
  <div class="modal" id="modal"><div class="modal-card"><header><h2 id="modalTitle">表单</h2><button class="ghost" id="closeModal" type="button">关闭</button></header><div class="modal-body" id="modalBody"></div></div></div>
<script>
const sections=[['overview','概览'],['providers','模型配置'],['agents','Agent 管理'],['runs','调用记录'],['tokens','Token / 租约'],['audit','审计']];
const state={last:null,agents:[],providers:[],runs:[],tokens:[],memories:[],conversation:null,audit:[],queue:null,health:null,modal:null,activeAgentId:null,agentChatMode:false,agentMemoryMode:false,chatSettings:{agentId:localStorage.getItem('vibe-claw-chat-agent-id')||'',compression:localStorage.getItem('vibe-claw-chat-compression')||'hybrid',chatType:localStorage.getItem('vibe-claw-chat-type')||'text'}};
const $=(id)=>document.getElementById(id);
function esc(v){return String(v??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}
function token(){return localStorage.getItem('vibe-claw-token')||'dev-token'}
function setResultHidden(hidden){$('result').classList.toggle('hidden',hidden);$('showResult').classList.toggle('hidden',!hidden);localStorage.setItem('vibe-claw-result-hidden',hidden?'1':'0')}
function show(data){state.last=data;$('out').textContent=JSON.stringify(data,null,2);if(localStorage.getItem('vibe-claw-result-hidden')!=='1')setResultHidden(false)}
async function api(path,opt={},silent=false){const res=await fetch(path,{...opt,headers:{'content-type':'application/json','authorization':'Bearer '+token(),...(opt.headers||{})}});let body;try{body=await res.json()}catch{body={status:res.status}};if(!res.ok)body={error:true,status:res.status,...body};if(!silent)show(body);return body}
function values(form){const o={};new FormData(form).forEach((v,k)=>{if(v===''||k==='attachments')return;if(['scopes','allowedProtocols','agentIds'].includes(k))o[k]=String(v).split(',').map(x=>x.trim()).filter(Boolean);else if(['maxCalls','tokenBudget','timeoutMs','maxRetries'].includes(k))o[k]=Number(v);else if(k==='baseUrl'||k==='apiKeyRef'||k==='providerId')o[k]=v||null;else o[k]=v});return o}
function item(title,meta,badge){return '<div class="item"><div class="item-head"><div><h3>'+esc(title)+'</h3><div class="code">'+esc(meta||'')+'</div></div>'+(badge?'<span class="badge">'+esc(badge)+'</span>':'')+'</div></div>'}
function badge(status){return '<span class="badge '+(status==='failed'||status==='disabled'?'bad':'')+'">'+esc(status||'-')+'</span>'}
function maskSecret(v){v=String(v||'-');if(v==='-')return '-';if(v.length<=8)return v[0]+'***'+v.at(-1);return v.slice(0,4)+'***********************'+v.slice(-4)}
function table(headers,rows,empty){if(!rows.length)return '<div class="table-wrap"><div class="empty">'+esc(empty)+'</div></div>';return '<div class="table-wrap"><table class="data-table"><thead><tr>'+headers.map(h=>'<th>'+esc(h)+'</th>').join('')+'</tr></thead><tbody>'+rows.join('')+'</tbody></table></div>'}
function row(cells,attrs=''){return '<tr'+(attrs?' '+attrs:'')+'>'+cells.map(c=>'<td>'+c+'</td>').join('')+'</tr>'}
function openModal(title,body){$('modalTitle').textContent=title;$('modalBody').innerHTML=body;$('modal').classList.add('open');bindModalForms()}
function closeModal(){$('modal').classList.remove('open');$('modalBody').innerHTML=''}
function renderShell(){$('nav').innerHTML=sections.map(([id,label])=>'<button type="button" data-tab="'+id+'">'+label+'</button>').join('')+'<button type="button" data-token-settings>访问令牌</button>';$('sections').innerHTML=sections.map(([id])=>'<section class="section" id="sec-'+id+'"></section>').join('');document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>activate(b.dataset.tab));document.querySelectorAll('[data-token-settings]').forEach(b=>b.onclick=tokenSettingsModal);activate('overview')}
function activate(id){state.agentChatMode=false;state.agentMemoryMode=false;document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===id));document.querySelectorAll('.section').forEach(s=>{const active=s.id==='sec-'+id;s.classList.toggle('active',active);s.classList.remove('chat-page');if(!active)s.innerHTML=''});$('sectionTitle').textContent=sections.find(x=>x[0]===id)?.[1]||id;$('chatSettingsTop').classList.add('hidden');renderSection(id)}
function overviewHero(){return '<section class="hero"><div class="hero-card"><span class="eyebrow">LIVE ADMIN · Vibe Claw 控制台</span><h1>模型、Agent、运行记录和记忆的统一运维台</h1><p class="muted">所有操作通过公开 API 完成，适合联调、排障和最小运营。敏感 token 只保存在当前浏览器。</p></div><div class="hero-card status-grid"><div class="metric"><b>'+esc(state.agents.length)+'</b><span>Agents</span></div><div class="metric"><b>'+esc(state.runs.length)+'</b><span>Runs</span></div><div class="metric"><b>'+esc(state.queue?.persisted?.queued??state.queue?.pending??'-')+'</b><span>Queue Pending</span></div><div class="metric"><b>'+esc(state.health?.store?.type||'-')+'</b><span>Store</span></div></div></section>'}
function renderSection(id){const el=$('sec-'+id);if(id==='overview')el.innerHTML=overviewHero()+'<div class="grid"><div class="panel"><h2>系统状态</h2><p class="muted">健康检查、队列和存储状态。</p><div class="list">'+item('服务',state.health?.service||'-',state.health?.ok?'ok':'unknown')+item('Provider',state.health?.provider||'-')+item('Store',JSON.stringify(state.health?.store||{}))+item('Queue',JSON.stringify(state.queue||{}))+'</div></div><div class="panel"><h2>快速操作</h2><div class="split-actions"><button data-action="refresh-all">刷新全部</button><button class="secondary" data-action="openapi">查看 OpenAPI</button><button class="secondary" data-action="health">健康检查</button></div></div></div>';
if(id==='providers')el.innerHTML='<div class="section-head"><p class="muted">管理模型供应商配置，密钥仅通过环境变量引用。</p><button data-modal="provider-create">新增 Provider</button></div>'+table(['名称','类型','模型','API Key Ref','状态','操作'],state.providers.map(p=>row([esc(p.name),esc(providerTypeLabel(p.type)),esc(p.defaultModel),'<span>'+esc(maskSecret(p.apiKeyRef||'-'))+'</span>',badge(p.status),'<div class="row-actions"><button class="small secondary" data-modal="provider-edit" data-id="'+p.id+'">修改</button></div>'])),'暂无 Provider，点击新增。');
if(id==='agents'){el.classList.toggle('chat-page',state.agentChatMode||state.agentMemoryMode);if(state.agentChatMode){el.innerHTML=renderAgentChatPage();$('sectionTitle').textContent=(state.agents.find(a=>a.id===state.activeAgentId)?.name||'Agent')+' · 对话';}else if(state.agentMemoryMode){el.innerHTML=renderAgentMemoryPage();$('sectionTitle').textContent=(state.agents.find(a=>a.id===state.activeAgentId)?.name||'Agent')+' · 记忆';}else el.innerHTML='<div class="section-head"><p class="muted">创建、修改和查看 Agent。点击对话或记忆可直接进入对应工作页。</p><button data-modal="agent-create">新增 Agent</button></div>'+table(['名称','模型','Provider','状态','Instruction','操作'],state.agents.map(a=>row([esc(a.name),esc(a.defaultModel),esc(a.providerId||'-'),badge(a.status),'<span class="code">'+esc((a.instruction||'').slice(0,120))+'</span>','<div class="row-actions"><button class="small secondary" data-agent-chat-page="'+a.id+'">对话</button><button class="small secondary" data-agent-memory-page="'+a.id+'">记忆</button><button class="small ghost" data-modal="agent-edit" data-id="'+a.id+'">修改</button></div>'])),'暂无 Agent，点击新增。');}
if(id==='conversations')el.innerHTML=renderChat();
if(id==='runs')el.innerHTML='<div class="section-head"><p class="muted">异步 Run 列表展示 Agent、最近调用时间、输入、输出和状态。点击记录可进入对应对话历史；无会话的 Run 会打开详情。</p><button data-modal="run-create">创建 Run</button></div>'+table(['Run','Agent','最近调用','输入','输出','Tokens','状态'],state.runs.map(r=>row(['<span class="code">'+esc(r.id)+'</span>','<span class="code">'+esc(runAgentLabel(r))+'</span>',esc(formatTime(r.lastCallAt||r.updatedAt||r.createdAt)||'-'),'<span class="code">'+esc(r.input)+'</span>','<span class="code">'+esc(r.output||'-')+'</span>',esc(r.totalTokens||0),badge(r.status)],'class="clickable" data-run-id="'+esc(r.id)+'" data-run-agent="'+esc(r.agentId||'')+'" data-run-conversation="'+esc(r.conversationId||'')+'" title="点击查看对话历史或 Run 详情"')),'暂无 Run，点击创建。');
if(id==='memories')el.innerHTML='<div class="section-head"><div class="split-actions"><button data-modal="memory-create">写入记忆</button><button class="secondary" data-modal="memory-query">查询记忆</button></div></div>'+table(['摘要','类型','内容','状态'],state.memories.map(m=>row([esc(m.summary),esc(m.type),'<span class="code">'+esc((m.content||'').slice(0,160))+'</span>',badge(m.status)])),'暂无记忆，写入或查询指定 Agent。');
if(id==='tokens')el.innerHTML='<div class="section-head"><div class="split-actions"><button data-modal="token-create">创建 Token</button><button class="secondary" data-modal="lease-create">创建租约</button></div></div>'+table(['名称','Scopes','状态','Token ID'],state.tokens.map(t=>row([esc(t.name),'<span class="code">'+esc((t.scopes||[]).join(','))+'</span>',badge(t.status),'<span class="code">'+esc(t.id)+'</span>'])),'暂无 Token，点击创建。');
if(id==='audit')el.innerHTML='<div class="grid"><div class="panel"><h2>审计事件</h2>'+table(['动作','目标','状态'],state.audit.slice(0,50).map(a=>row([esc(a.action),'<span class="code">'+esc(a.targetType+': '+a.targetId)+'</span>',badge(a.status)])),'暂无审计，点击刷新。')+'</div><div class="panel"><h2>队列</h2><pre class="code">'+esc(JSON.stringify(state.queue,null,2))+'</pre></div></div>';
bindSectionActions(id)}
function renderChat(){const messages=chatMessages();const cfg=chatSettings();return '<div class="conversation-view"><div class="chat-shell"><div class="chat-history" id="chatHistory">'+(messages.length?messages.map(chatBubble).join(''):'<div class="bubble system"><div class="bubble-meta">系统</div><div class="bubble-text">暂无聊天记录。选择 Agent 后发送消息即可开始。</div></div>')+'</div><form id="fMessage" class="chat-composer"><input type="hidden" name="agentId" value="'+esc(cfg.agentId)+'"><input type="hidden" name="compression" value="'+esc(cfg.compression)+'"><input type="hidden" name="chatType" value="'+esc(cfg.chatType)+'"><div class="composer-mini"><label class="composer-attach">附件<input name="attachments" type="file" multiple></label><div class="composer-message"><textarea name="message" aria-label="消息" placeholder="输入消息，Enter 发送，Ctrl+Enter 换行"></textarea></div><button>发送</button></div><div class="attachments">当前 Agent：<span class="code">'+esc(cfg.agentId||'未选择')+'</span> · 附件会作为模型上下文发送。</div></form></div></div>'}
function renderAgentChatPage(){const agent=state.agents.find(a=>a.id===state.activeAgentId);return '<div class="section-head"><p class="muted">'+esc(agent?.name||'Agent')+' 的历史消息按时间展示，可直接继续对话。</p><button class="secondary" type="button" data-agent-back>返回 Agent 列表</button></div>'+renderChat()}
function renderAgentMemoryPage(){const agent=state.agents.find(a=>a.id===state.activeAgentId);return '<div class="section-head"><p class="muted">'+esc(agent?.name||'Agent')+' 的长期记忆，可直接查看并写入新记忆。</p><button class="secondary" type="button" data-agent-back>返回 Agent 列表</button></div><div class="panel"><form id="fAgentMemoryPage" class="form-grid"><label>类型<select name="type"><option>profile</option><option>semantic</option><option>episodic</option><option>working</option></select></label><label>摘要<input name="summary"></label><label class="wide">内容<textarea name="content"></textarea></label><input type="hidden" name="source" value="admin"><div class="modal-actions wide"><button>写入记忆</button></div></form></div><div style="margin-top:14px">'+table(['摘要','类型','内容','状态'],state.memories.map(m=>row([esc(m.summary),esc(m.type),'<span class="code">'+esc((m.content||'').slice(0,160))+'</span>',badge(m.status)])),'暂无记忆')+'</div>'}
function chatMessages(){if(state.conversation?.messages)return state.conversation.messages;if(state.conversation?.userMessage&&state.conversation?.message)return [state.conversation.userMessage,state.conversation.message];return []}
function formatTime(v){if(!v)return '';const d=new Date(v);return Number.isNaN(d.getTime())?'':d.toLocaleString()}
function runAgentLabel(r){return r.agentName||r.agentId||'-'}
function runFallbackMessages(r){const messages=[{role:'user',content:r.input||'',createdAt:r.createdAt}];if(r.output)messages.push({role:'agent',content:r.output,createdAt:r.updatedAt,totalTokens:r.totalTokens});else messages.push({role:'system',content:r.errorMessage||('Run 状态：'+(r.status||'-')),createdAt:r.updatedAt});return messages}
function showAgentsWorkSection(){document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab==='agents'));document.querySelectorAll('.section').forEach(s=>{const active=s.id==='sec-agents';s.classList.toggle('active',active);s.classList.toggle('chat-page',active)});renderSection('agents');bindSectionActions('agents')}
async function openRunRecord(runId){const run=state.runs.find(r=>r.id===runId);if(!run){const body=await api('/v1/runs/'+runId,{},true);show(body);return}if(run.agentId){state.activeAgentId=run.agentId;state.agentChatMode=true;state.agentMemoryMode=false;state.chatSettings.agentId=run.agentId;localStorage.setItem('vibe-claw-chat-agent-id',run.agentId);state.conversation=run.conversationId?await api('/v1/conversations/'+run.conversationId,{},true):{conversation:null,messages:runFallbackMessages(run)};showAgentsWorkSection();return}const detail=await api('/v1/runs/'+runId,{},true);show(detail);openModal('Run 详情','<pre class="code">'+esc(JSON.stringify(detail,null,2))+'</pre>')}
function chatBubble(m){const role=m.role||'system';const time=formatTime(m.createdAt);const meta=esc(role)+(time?' · '+esc(time):'')+(m.totalTokens?' · '+esc(m.totalTokens)+' tokens':'');return '<div class="bubble '+esc(role)+'"><div class="bubble-meta">'+meta+'</div><div class="bubble-text">'+esc(m.content||'')+'</div></div>'}
function chatSettings(){if(!state.chatSettings.agentId&&state.agents[0])state.chatSettings.agentId=state.agents[0].id;return state.chatSettings}
function chatSettingsForm(){const cfg=chatSettings();openModal('会话设置','<form id="modalChatSettings" class="form-grid"><label>Agent<select name="agentId">'+state.agents.map(a=>'<option value="'+esc(a.id)+'" '+(a.id===cfg.agentId?'selected':'')+'>'+esc(a.name)+' · '+esc(a.id)+'</option>').join('')+'</select></label><label>Compression<select name="compression"><option '+(cfg.compression==='hybrid'?'selected':'')+'>hybrid</option><option '+(cfg.compression==='none'?'selected':'')+'>none</option><option '+(cfg.compression==='recent_only'?'selected':'')+'>recent_only</option><option '+(cfg.compression==='rolling_summary'?'selected':'')+'>rolling_summary</option></select></label><label>聊天类型<select name="chatType"><option value="text" '+(cfg.chatType==='text'?'selected':'')+'>文本对话</option><option value="context" '+(cfg.chatType==='context'?'selected':'')+'>带上下文</option><option value="multimodal" '+(cfg.chatType==='multimodal'?'selected':'')+'>附件/多模态上下文</option></select></label><label class="wide">Conversation ID<input name="conversationId" placeholder="conv_xxx，留空则保存设置"></label><div class="modal-actions wide"><button>保存/查询</button></div></form>')}
function bindSectionActions(id){document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>{if(b.dataset.action==='refresh-all')loadAll();if(b.dataset.action==='openapi')api('/openapi.json');if(b.dataset.action==='health')api('/health',{headers:{authorization:undefined}})});document.querySelectorAll('[data-modal]').forEach(b=>b.onclick=()=>openEntityModal(b.dataset.modal,b.dataset.id));document.querySelectorAll('[data-agent-chat-page]').forEach(b=>b.onclick=()=>openAgentChatPage(b.dataset.agentChatPage));document.querySelectorAll('[data-agent-memory-page]').forEach(b=>b.onclick=()=>openAgentMemoryPage(b.dataset.agentMemoryPage));document.querySelectorAll('[data-run-id]').forEach(r=>r.onclick=()=>openRunRecord(r.dataset.runId));document.querySelectorAll('[data-agent-back]').forEach(b=>b.onclick=()=>{state.agentChatMode=false;state.agentMemoryMode=false;state.conversation=null;$('sectionTitle').textContent='Agent 管理';renderSection('agents')});const on=(fid,fn)=>{const f=$(fid);if(f)f.onsubmit=async e=>{e.preventDefault();if(f.dataset.sending==='1')return;setFormBusy(f,true);try{await fn(f)}finally{setFormBusy(f,false)}}};on('fConversation',async f=>{state.conversation=await api('/v1/conversations/'+new FormData(f).get('id'));renderSection('conversations')});on('fMessage',sendMessage);on('fAgentMemoryPage',writeAgentMemoryPage);bindEnterToSend($('fMessage'));autoSizeTextareas()}
async function sendMessage(f){const v=values(f);const id=v.agentId;const chatType=v.chatType;delete v.agentId;delete v.chatType;if(state.conversation?.conversation?.id)v.conversationId=state.conversation.conversation.id;const attachmentContext=await filesToContext(f.elements.attachments?.files||[]);v.context=[...(v.context||[]),{source:'system',content:'chat_type:'+chatType,priority:40},...attachmentContext];const body=await api('/v1/agents/'+id+'/messages',{method:'POST',body:JSON.stringify(v)});if(body.conversation?.id){const full=await api('/v1/conversations/'+body.conversation.id,{},true);state.conversation={...body,...full}}else state.conversation=body;await loadAll();renderSection(state.agentChatMode?'agents':'conversations')}
async function filesToContext(files){const out=[];for(const file of Array.from(files)){if(file.type.startsWith('text/')||/\.(md|json|txt|csv|log)$/i.test(file.name)){const text=(await file.text()).slice(0,12000);out.push({source:'user',content:'附件 '+file.name+' ('+file.type+', '+file.size+' bytes):\\n'+text,priority:60})}else{out.push({source:'user',content:'附件元数据 '+file.name+'; type='+(file.type||'unknown')+'; size='+file.size+' bytes。当前模型接口按文本上下文发送该附件元数据。',priority:45})}}return out}
function tokenSettingsModal(){openModal('API 访问令牌','<form id="modalTokenSettings" class="form-grid"><label class="wide">访问令牌<input name="token" value="'+esc(token())+'" placeholder="dev-token"></label><p class="muted wide">用于访问后台 API。请求会自动携带 Authorization: Bearer 该令牌。令牌只保存在当前浏览器。</p><div class="modal-actions wide"><button class="secondary" type="button" id="modalRefreshAll">刷新数据</button><button>保存令牌</button></div></form>');const r=$('modalRefreshAll');if(r)r.onclick=()=>loadAll()}
function openEntityModal(kind,id){const map={
'provider-create':()=>providerForm('创建 Provider'), 'provider-edit':()=>providerForm('修改 Provider',state.providers.find(x=>x.id===id)),
'agent-create':()=>agentForm('创建 Agent'), 'agent-edit':()=>agentForm('修改 Agent',state.agents.find(x=>x.id===id)),
'run-create':()=>runForm(), 'memory-create':()=>memoryForm(), 'memory-query':()=>memoryQueryForm(), 'token-create':()=>tokenForm(), 'lease-create':()=>leaseForm(), 'chat-settings':()=>chatSettingsForm(), 'agent-chat':()=>agentConversationsModal(id), 'agent-memory':()=>agentMemoryModal(id)
};if(map[kind])map[kind]()}
async function openAgentChatPage(agentId){state.activeAgentId=agentId;state.agentChatMode=true;state.agentMemoryMode=false;state.chatSettings.agentId=agentId;localStorage.setItem('vibe-claw-chat-agent-id',agentId);const body=await api('/v1/agents/'+agentId+'/conversations?limit=1',{},true);const latest=(body.conversations||[])[0];state.conversation=latest?await api('/v1/conversations/'+latest.id,{},true):{conversation:null,messages:[]};renderSection('agents')}
async function openAgentMemoryPage(agentId){state.activeAgentId=agentId;state.agentChatMode=false;state.agentMemoryMode=true;const body=await api('/v1/agents/'+agentId+'/memories',{},true);state.memories=body.memories||[];renderSection('agents')}
async function writeAgentMemoryPage(f){const body=await api('/v1/agents/'+state.activeAgentId+'/memories',{method:'POST',body:JSON.stringify(values(f))});if(body.memory)await openAgentMemoryPage(state.activeAgentId)}
async function agentConversationsModal(agentId){state.activeAgentId=agentId;const agent=state.agents.find(a=>a.id===agentId);const body=await api('/v1/agents/'+agentId+'/conversations?limit=10',{},true);const items=body.conversations||[];openModal((agent?.name||'Agent')+' · 最近对话','<div class="agent-dialog"><div class="split-actions"><button type="button" data-agent-new-chat="'+esc(agentId)+'">开始新对话</button></div><div class="recent-list">'+(items.length?items.map(c=>'<div class="recent-card"><span><span class="recent-preview">'+esc(c.preview||'暂无内容')+'</span><span class="recent-meta">'+esc(new Date(c.updatedAt).toLocaleString())+' · '+esc(c.messageCount||0)+' 条消息 · '+esc(c.id)+'</span></span><button type="button" class="small" data-open-agent-chat="'+esc(agentId)+'" data-conv="'+esc(c.id)+'">进入</button></div>').join(''):'<div class="empty">暂无最近对话，点击开始新对话。</div>')+'</div></div>');bindAgentDialogActions()}
async function openAgentChat(agentId,conversationId){state.activeAgentId=agentId;state.chatSettings.agentId=agentId;localStorage.setItem('vibe-claw-chat-agent-id',agentId);state.conversation=conversationId?await api('/v1/conversations/'+conversationId,{},true):{conversation:null,messages:[]};renderAgentChatModal(agentId)}
function renderAgentChatModal(agentId){const agent=state.agents.find(a=>a.id===agentId);const messages=(state.conversation?.messages||[]).slice().sort((a,b)=>a.createdAt.localeCompare(b.createdAt));openModal((agent?.name||'Agent')+' · 对话窗口','<div class="agent-chat-box"><div class="chat-history">'+(messages.length?messages.map(chatBubble).join(''):'<div class="bubble system"><div class="bubble-meta">系统</div><div class="bubble-text">暂无消息，输入后开始新会话。</div></div>')+'</div><form id="modalAgentMessage" class="modal-chat-composer"><textarea name="message" aria-label="消息" placeholder="输入消息，Enter 发送，Ctrl+Enter 换行"></textarea><button>发送</button></form></div>');bindAgentDialogActions();autoSizeTextareas()}
async function agentMemoryModal(agentId){state.activeAgentId=agentId;const agent=state.agents.find(a=>a.id===agentId);const body=await api('/v1/agents/'+agentId+'/memories',{},true);state.memories=body.memories||[];openModal((agent?.name||'Agent')+' · 记忆管理','<div class="agent-dialog"><form id="modalAgentMemory" class="form-grid"><label>类型<select name="type"><option>profile</option><option>semantic</option><option>episodic</option><option>working</option></select></label><label>摘要<input name="summary"></label><label class="wide">内容<textarea name="content"></textarea></label><input type="hidden" name="source" value="admin"><div class="modal-actions wide"><button>写入记忆</button></div></form>'+table(['摘要','类型','内容','状态'],state.memories.map(m=>row([esc(m.summary),esc(m.type),'<span class="code">'+esc((m.content||'').slice(0,160))+'</span>',badge(m.status)])),'暂无记忆')+'</div>');bindAgentDialogActions()}
function bindAgentDialogActions(){document.querySelectorAll('[data-open-agent-chat]').forEach(b=>b.onclick=()=>openAgentChat(b.dataset.openAgentChat,b.dataset.conv));document.querySelectorAll('[data-agent-new-chat]').forEach(b=>b.onclick=()=>openAgentChat(b.dataset.agentNewChat,''));const msg=$('modalAgentMessage');if(msg){bindEnterToSend(msg);msg.onsubmit=async e=>{e.preventDefault();if(msg.dataset.sending==='1')return;setFormBusy(msg,true);try{const v=values(msg);const agentId=state.activeAgentId;const payload={message:v.message,compression:state.chatSettings.compression,context:[{source:'system',content:'chat_type:'+state.chatSettings.chatType,priority:40}]};if(state.conversation?.conversation?.id)payload.conversationId=state.conversation.conversation.id;const body=await api('/v1/agents/'+agentId+'/messages',{method:'POST',body:JSON.stringify(payload)});if(body.conversation?.id)state.conversation=await api('/v1/conversations/'+body.conversation.id,{},true);await loadAll();renderAgentChatModal(agentId)}finally{setFormBusy(msg,false)}}}const mem=$('modalAgentMemory');if(mem)mem.onsubmit=async e=>{e.preventDefault();const body=await api('/v1/agents/'+state.activeAgentId+'/memories',{method:'POST',body:JSON.stringify(values(mem))});if(body.memory)await agentMemoryModal(state.activeAgentId)}}
function bindEnterToSend(form){if(!form)return;const t=form.querySelector('textarea[name="message"]');if(!t)return;t.onkeydown=e=>{if(e.key==='Enter'&&!e.ctrlKey){e.preventDefault();if(form.requestSubmit)form.requestSubmit();else form.dispatchEvent(new Event('submit',{cancelable:true,bubbles:true}))}}}
function setFormBusy(form,busy){form.dataset.sending=busy?'1':'0';form.querySelectorAll('button').forEach(b=>{if(busy){b.dataset.label=b.textContent;b.textContent='发送中...';b.disabled=true}else{b.disabled=false;if(b.dataset.label)b.textContent=b.dataset.label}})}
function autoSizeTextareas(){document.querySelectorAll('textarea').forEach(t=>{const fit=()=>{t.style.height='42px';t.style.height=Math.min(t.scrollHeight,160)+'px'};t.oninput=fit;fit()})}
function providerTypeLabel(type){return type==='mock'?'本地测试模型（Mock）':type==='openai-compatible'?'真实大模型（OpenAI 兼容）':type}
function providerForm(title,p){openModal(title,'<form id="modalProvider" class="form-grid" data-id="'+esc(p?.id||'')+'"><label>名称<input name="name" value="'+esc(p?.name||'')+'" placeholder="DeepSeek"></label><label>类型<select name="type"><option value="openai-compatible" '+(p?.type!=='mock'?'selected':'')+'>真实大模型（DeepSeek / OpenAI 兼容）</option><option value="mock" '+(p?.type==='mock'?'selected':'')+'>本地测试模型（不调用真实大模型）</option></select><span class="code">真实大模型用于 DeepSeek、OpenAI 兼容接口；本地测试模型只返回假数据，适合离线调试。</span></label><label>Base URL<input name="baseUrl" value="'+esc(p?.baseUrl||'')+'" placeholder="https://api.deepseek.com/v1"></label><label>默认模型<input name="defaultModel" value="'+esc(p?.defaultModel||'deepseek-chat')+'"></label><label>API Key Ref<input name="apiKeyRef" value="'+esc(p?.apiKeyRef||'DEEPSEEK_API_KEY')+'"></label><label>重试次数<input name="maxRetries" value="'+esc(p?.maxRetries??2)+'"></label><div class="modal-actions wide"><button>保存</button></div></form>')}
function providerOptions(selected){return '<option value="">使用默认 Provider</option>'+state.providers.map(p=>'<option value="'+esc(p.id)+'" '+(p.id===selected?'selected':'')+'>'+esc(p.name)+' · '+esc(p.defaultModel)+' · '+esc(p.id)+'</option>').join('')}
function providerHint(){return state.providers.length?'选择已创建的 Provider；不要手填随机 ID。':'当前没有 Provider，可留空使用默认配置，或先到模型配置创建。'}
function modelOptions(selected){const models=[...new Set(state.providers.map(p=>p.defaultModel).filter(Boolean))];if(!models.length)return '<option value="" disabled selected>请先在模型配置中创建大模型</option>';const current=selected&&models.includes(selected)?selected:models[0];return models.map(m=>'<option value="'+esc(m)+'" '+(m===current?'selected':'')+'>'+esc(m)+'</option>').join('')}
function modelHint(){return state.providers.length?'只能选择已在模型配置中登记的 Provider 默认模型。':'没有可选模型，请先到模型配置新增 Provider。'}
function agentForm(title,a){openModal(title,'<form id="modalAgent" class="form-grid" data-id="'+esc(a?.id||'')+'"><label>名称<input name="name" value="'+esc(a?.name||'')+'"></label><label>默认模型<select name="defaultModel" '+(state.providers.length?'':'disabled')+'>'+modelOptions(a?.defaultModel||'')+'</select><span class="code">'+esc(modelHint())+'</span></label><label>Provider<select name="providerId">'+providerOptions(a?.providerId||'')+'</select><span class="code">'+esc(providerHint())+'</span></label><label class="wide">Instruction<textarea name="instruction">'+esc(a?.instruction||'')+'</textarea></label><div class="modal-actions wide"><button '+(state.providers.length?'':'disabled')+'>保存</button></div></form>')}
function runForm(){openModal('创建 Run','<form id="modalRun" class="form-grid"><label>Agent IDs<input name="agentIds" placeholder="agent_a,agent_b"></label><label>Provider<select name="providerId">'+providerOptions('')+'</select><span class="code">可选。留空时使用 Agent 绑定 Provider 或默认 Provider。</span></label><label class="wide">Input<textarea name="input"></textarea></label><div class="modal-actions wide"><button>创建</button></div></form>')}
function memoryForm(){openModal('写入记忆','<form id="modalMemory" class="form-grid"><label>Agent ID<input name="agentId"></label><label>类型<select name="type"><option>profile</option><option>semantic</option><option>episodic</option><option>working</option></select></label><label>摘要<input name="summary"></label><label>来源<input name="source" value="admin"></label><label class="wide">内容<textarea name="content"></textarea></label><div class="modal-actions wide"><button>写入</button></div></form>')}
function memoryQueryForm(){openModal('查询记忆','<form id="modalMemoryQuery" class="form-grid"><label>Agent ID<input name="agentId"></label><div class="modal-actions wide"><button>查询</button></div></form>')}
function tokenForm(){openModal('创建 Token','<form id="modalToken" class="form-grid"><label>名称<input name="name"></label><label>Scopes<input name="scopes" placeholder="agents:read,runs:read"></label><div class="modal-actions wide"><button>创建</button></div></form>')}
function leaseForm(){openModal('创建租约','<form id="modalLease" class="form-grid"><label>Agent ID<input name="agentId"></label><label>过期时间<input name="expiresAt" placeholder="2026-12-31T00:00:00.000Z"></label><label>调用次数<input name="maxCalls" value="10"></label><label>Token 预算<input name="tokenBudget" value="100000"></label><label class="wide">允许协议<input name="allowedProtocols" placeholder="vibe-example/v1"></label><div class="modal-actions wide"><button>创建</button></div></form>')}
function bindModalForms(){const on=(fid,fn)=>{const f=$(fid);if(f)f.onsubmit=async e=>{e.preventDefault();await fn(f);closeModal();await loadAll()}};on('modalTokenSettings',f=>{localStorage.setItem('vibe-claw-token',new FormData(f).get('token')||'dev-token');show({ok:true,message:'API 访问令牌已保存到当前浏览器'});return {ok:true}});on('modalProvider',f=>{const id=f.dataset.id;return api(id?'/v1/providers/'+id:'/v1/providers',{method:id?'PATCH':'POST',body:JSON.stringify(values(f))})});on('modalAgent',f=>{const id=f.dataset.id;return api(id?'/v1/agents/'+id:'/v1/agents',{method:id?'PATCH':'POST',body:JSON.stringify(values(f))})});on('modalRun',f=>api('/v1/runs',{method:'POST',body:JSON.stringify(values(f))}));on('modalMemory',async f=>{const v=values(f);const id=v.agentId;delete v.agentId;const body=await api('/v1/agents/'+id+'/memories',{method:'POST',body:JSON.stringify(v)});if(body.memory)state.memories=[body.memory,...state.memories.filter(m=>m.id!==body.memory.id)]});on('modalMemoryQuery',async f=>{const body=await api('/v1/agents/'+new FormData(f).get('agentId')+'/memories');state.memories=body.memories||[]});on('modalToken',f=>api('/v1/tokens',{method:'POST',body:JSON.stringify(values(f))}));on('modalLease',f=>{const v=values(f);const id=v.agentId;delete v.agentId;return api('/v1/agents/'+id+'/leases',{method:'POST',body:JSON.stringify(v)})});on('modalChatSettings',async f=>{const v=values(f);state.chatSettings={agentId:v.agentId||'',compression:v.compression||'hybrid',chatType:v.chatType||'text'};localStorage.setItem('vibe-claw-chat-agent-id',state.chatSettings.agentId);localStorage.setItem('vibe-claw-chat-compression',state.chatSettings.compression);localStorage.setItem('vibe-claw-chat-type',state.chatSettings.chatType);const cid=new FormData(f).get('conversationId');if(cid)state.conversation=await api('/v1/conversations/'+cid);renderSection('conversations');return {ok:true}})}
async function loadAll(){state.health=await api('/health',{headers:{authorization:undefined}},true);state.agents=(await api('/v1/agents',{},true)).agents||[];state.providers=(await api('/v1/providers',{},true)).providers||[];state.runs=(await api('/v1/runs',{},true)).runs||[];state.tokens=(await api('/v1/tokens',{},true)).tokens||[];state.audit=(await api('/v1/audit-events',{},true)).auditEvents||[];state.queue=(await api('/v1/queue',{},true)).queue||null;renderSection(document.querySelector('.section.active')?.id.replace('sec-','')||'overview')}
$('hideResult').onclick=()=>setResultHidden(true);$('showResult').onclick=()=>setResultHidden(false);$('chatSettingsTop').onclick=()=>openEntityModal('chat-settings');$('closeModal').onclick=closeModal;$('modal').onclick=(e)=>{if(e.target===$('modal'))closeModal()};$('copyLast').onclick=()=>navigator.clipboard?.writeText(JSON.stringify(state.last,null,2));setResultHidden(localStorage.getItem('vibe-claw-result-hidden')==='1');renderShell();loadAll();
</script>
</body>
</html>`;
