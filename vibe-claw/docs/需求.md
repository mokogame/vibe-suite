提交前代码审查
检查这轮大改有没有临时实现、重复逻辑、命名不稳、过宽的类型、测试只覆盖 happy path 的问题。

整理迁移
现在很多表都追加在 001_initial.sql，如果这是未发布项目可以接受；如果已经有人跑过迁移，应该拆成 002_* 增量迁移。

补 OpenAPI 完整 schema
目前 OpenAPI 还不是完整契约，新增的普通对话、协议、记忆、租约、后台入口等接口需要完整 request/response schema。

Provider 配置真正接入运行时
现在 provider 配置可管理，但模型调用仍主要走环境变量 provider。应支持 Agent 或 Run 选择 provider config。

后台 UI 从入口页变成可操作页面
现在 /admin 是控制台入口，不是完整管理界面。应补模型配置、Agent、会话、调用记录、记忆、Token/租约页面。

持久化队列
当前恢复机制是把未终态 Run 标记失败，不是断点续跑。生产上应引入 DB 队列或 BullMQ。

